spring:
  application:
    name: licensingservice  # must match the name of the directory within Spring Cloud config server
  profiles:
    active:
      default
  cloud:
    config:
      uri: http://localhost:8888